steps:
  # Step 1: Build container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/streamlit-ml-demo', '.']

  # Step 2: Push image to Container Registry (or Artifact Registry)
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/streamlit-ml-demo']

   # Step 3: Deploy to Cloud Run (commented out for now)
  # - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  #   entrypoint: gcloud
  #   args:
  #     [
  #       'run', 'deploy', 'streamlit-ml-demo',
  #       '--image', 'gcr.io/$PROJECT_ID/streamlit-ml-demo',
  #       '--region', 'us-central1',
  #       '--platform', 'managed',
  #       '--allow-unauthenticated',
  #     ]

# The image built by this pipeline
images:
  - 'gcr.io/$PROJECT_ID/streamlit-ml-demo'

options:
  logging: CLOUD_LOGGING_ONLY
